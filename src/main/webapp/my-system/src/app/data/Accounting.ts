import {Library} from "./Library";
import {Reader} from "./Reader";

export class Accounting {
  id:	number;
  library: number;
  reader: number;
  dateExt:	string;
  dateRet:	string;
  status:	string;
  fine:	number;
}
